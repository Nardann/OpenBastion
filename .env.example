# PostgreSQL Configuration
POSTGRES_USER=bastion_user
POSTGRES_PASSWORD=bastion_password
POSTGRES_DB=bastion_db
POSTGRES_PORT=5432

# Database URL (constructed from PostgreSQL config above)
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public"

# JWT & Vault Configuration
# Générer avec: openssl rand -hex 32
# IMPORTANT : VAULT_KEY/VAULT_SALT chiffrent les Secret en DB. Si vous les
# tournez, vous devez ré-encrypter toutes les entrées Secret/AuthProvider
# existantes (sinon le déchiffrement échoue avec "Integrity breach").
JWT_SECRET="CHANGE_ME_GENERATE_WITH_openssl_rand_-hex_32"
VAULT_KEY="CHANGE_ME_GENERATE_WITH_openssl_rand_-hex_32"
VAULT_SALT="CHANGE_ME_GENERATE_WITH_openssl_rand_-hex_32"

# Server Configuration
FRONTEND_HTTPS_PORT=443
BACKEND_PORT=4000
NODE_ENV=production
CORS_ALLOWED_ORIGINS=https://localhost
RECORDINGS_PATH=/var/lib/bastion/recordings
ENABLE_RDP=false

# Default admin credentials (used UNIQUEMENT à la création initiale du compte
# si aucun admin n'existe en DB). >= 16 chars, complexité requise.
ADMIN_EMAIL=admin@bastion.local
ADMIN_PASSWORD="CHANGE_ME_MIN_16_CHARS_WITH_COMPLEXITY!"

# Prometheus metrics endpoint protection. SANS ce token, /api/metrics serait
# accessible sans auth.
METRICS_TOKEN="CHANGE_ME_GENERATE_WITH_openssl_rand_-hex_32"

# OIDC dev-only TLS bypass. NE PAS activer en production.
# OIDC_ALLOW_INSECURE_TLS=false

# Rate Limiting Configuration
# Global: applies to all requests
THROTTLE_GLOBAL_TTL=1000
THROTTLE_GLOBAL_LIMIT=20

# Auth: specific limits for login, otp, and sudo
# TTL in milliseconds (e.g., 900000 = 15 minutes)
THROTTLE_AUTH_TTL=900000
THROTTLE_AUTH_LIMIT=20
